 SELECT * FROM learning_mysql.usuarios;

SELECT MAX(age) FROM learning_mysql.usuarios;

-- For commands Min and Max  is not possible to bring up other information

SELECT MIN(age) FROM learning_mysql.usuarios;